#include <stdio.h>
// Quick Quiz: Write a program to print first ‘n’ natural numbers using for loop
int main() {
    int n = 5;
    // scanf("%d",&n);
    for (int i=n; i ; i--)
    printf("%d\n",i);
    
     return 0;
}
